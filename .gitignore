*.log
*.sql
*.sql.gz
.DS_Store
.env
.env.backup
.env.production
# Deployment bundle: live credentials, a database dump and family photos.
# Never belongs in the repository.
/deploy
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Published portfolio overrides. Written by the app beside each built page and
# different on every machine — tracking them would let a deploy overwrite what
# someone has just published from the live site.
portfolios/dist/*/data.json
